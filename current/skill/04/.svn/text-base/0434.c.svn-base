
inherit SKILL;

// 函数：构造处理
void create() 
{ 
        set_number(0434);
        set_name( "回避强化" );
}

int get_learn_money() {return 31000; }

// 函数：重计属性
void recount_char( object me ) { USER_ENERGY_D->count_dodge_rate(me); }

// 函数：获取描述
string get_desc() 
{
        return "［回避强化］ 帮派特殊技，可增加玩家的回避率，每级增加1.5％。\n";
}
