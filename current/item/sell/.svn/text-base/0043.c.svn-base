#include <equip.h>
#include <ansi.h>
inherit USABLE;
inherit ITEM;

int get_item_value() {return 20 ;}
int get_item_value_2() {return 20 ;}
int get_item_number() {return 10001070 ;}
int get_item_color() {return 1 ;}

// 函数：构造处理
void create()
{
        set_name("开门芝麻");
        set_picid_1(3234);
        set_picid_2(3234);
        set_value(40);
}

// 函数：获取描述
string get_desc()
{
        return "不同于寻常的芝麻，开花时是节节低，\n可不受等级限制进入比自己等级阶段低的副本场景。\n每次进入比自己等级阶段低的副本会自动消耗一个开门芝麻。";
}
